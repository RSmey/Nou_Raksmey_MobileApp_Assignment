<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.raksmey.nou_raksmey_mobileapp_assignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SplashScreenActivity"
            android:noHistory="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".log_form.SignInAcitity" />
        <activity android:name=".log_form.SignUpActivity" />
        <activity
            android:name=".NaviDrawerActivity"
            android:label="@string/title_activity_navi_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".pages.MainListviewActivity" />
        <activity android:name=".pages.ListProductActivity"></activity>
    </application>

</manifest>